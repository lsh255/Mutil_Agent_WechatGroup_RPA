# 多模态Agent自动化项目：架构设计文档 (LangGraph版)

## 1. 概述

### 1.1 项目愿景

构建一个基于LLM驱动的智能AI Agent系统，通过多模态理解和自主决策能力，实现微信工作群的自动化监控与任务管理。Agent能够识别用户意图、自动启动监控、视觉定位群聊区域、理解群聊消息内容，并自主决策后续操作（写日报、改台账、保存消息等），解决人工整理效率低下、易出错的痛点。

系统核心能力：
- **LLM意图识别**: 通过大语言模型自动识别用户对话意图，判断是配置任务还是启动群聊监控
- **自动启动监控**: 识别到监控指令后，自动启动微信沙盒监控程序
- **视觉定位监控区域**: 通过LLM视觉能力自动识别并定位微信界面中的群聊名称、消息接收区域、消息发送区域
- **流式消息获取**: 启动微信沙盒监控程序，流式获取群聊消息供后续处理
- **多模态消息理解**: 通过本地多模态LLM理解图文混合的群聊消息内容
- **Agent自主决策**: 核心AI Agent根据消息内容自主判断后续操作（写日报、改台账、保存消息等）
- **人机协同**: 微信扫码登录阶段返回交互层，引导用户辅助登录
- **分阶段演进**: 从单用户单群聊，逐步扩展到单用户多群聊，最终支持多用户场景

### 1.2 核心设计原则

**Orchestrator-Worker模式**
- 中心化协调器负责任务分发和结果汇总
- Worker节点专注于特定领域处理（意图识别、视觉定位、多模态理解、文档执行）
- 通过LangGraph Send API动态创建和分发Worker任务

**有状态工作流引擎**
- LangGraph管理的状态机，支持条件路由和循环控制
- 状态持久化通过Redis Checkpoint实现，支持工作流恢复
- 流式输出能力，实时返回Agent推理过程

**模块化工具集成**
- 执行模块封装为标准化工具，通过Tool Calling机制集成
- 工具层与工作流解耦，便于扩展和维护

**可观测性**
- 关键节点暴露处理日志和指标
- 支持LangSmith集成，便于调试和追踪

### 1.3 多阶段演进路线图

**阶段一：单用户+单群聊（MVP）**

目标：验证核心功能，完成基础流程闭环

功能范围：
- 单个用户通过对话式Web UI配置任务
- 支持配置单个微信工作群的监控
- 自动生成日报并更新本地的Excel台账
- 基本的任务状态跟踪

技术实现：
- 单个微信沙盒实例
- 单个工作流实例处理消息
- 简单的用户会话管理（内存存储）

**阶段二：单用户+多群聊映射**

目标：支持用户根据不同任务配置监控不同的群聊

功能范围：
- 用户可以配置多个任务，每个任务对应不同的群聊
- 消息1触发监控群聊1，消息2触发监控群聊2
- 支持动态切换监控的群聊
- 多群聊消息独立处理，互不干扰

技术实现：
- 扩展State中的`monitored_groups`为列表
- MonitorGroupParser Node支持解析多个群聊指令
- 多个工作流实例并行处理不同群聊的消息
- 群聊消息与任务的映射关系管理

**阶段三：多用户场景**

目标：支持多个独立用户使用系统

功能范围：
- 用户注册与登录
- 每个用户独立的任务配置和群聊监控
- 用户数据隔离（任务、台账、日报）
- 多用户并发访问

技术实现：
- 用户管理系统（身份认证、权限控制）
- State增加`user_id`字段，实现用户数据隔离
- Redis使用用户ID作为key前缀
- ChromaDB为每个用户创建独立的collection
- 多实例微信沙盒部署（每个用户一个实例）

## 2. 架构总览

系统采用基于LangGraph的Orchestrator-Worker架构。核心Agent决策节点作为Orchestrator负责任务分发，各功能节点作为Worker专注于特定领域处理，通过状态共享和条件路由协同工作。

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              用户交互层                                                     │
│                                                                                          │
│  ┌──────────────────────────────┐  ┌────────────────────────────────────────────────────────────────────────────┐   │
│  │    浏览器 noVNC (VNC客户端)    │  │          对话式 Web UI (Chat Interface)                                  │   │
│  │    ─────────────────────────   │  │          ───────────────────────────────────                            │   │
│  │    x11vnc + noVNC              │  │          React 18.3+ + Vite 5.4+ + TypeScript 5.5+                      │   │
│  │    远程访问微信桌面界面          │  │          Zustand 4.5+ + shadcn/ui + Tailwind CSS 3.4+                   │   │
│  │                                │  │          React Router 6.26+ + WebSocket + Axios                          │   │
│  │                                │  │                                                                           │   │
│  │  ┌────────────────────────┐    │  │  用户发送: "1.工作1（地点A）人员1，人员2"                               │   │
│  │  │ Fluxbox窗口管理器       │    │  │  用户发送: "监控群聊SSS"                                                │   │
│  │  │ 轻量级桌面环境          │    │  │  AI响应: 任务配置成功 / 已启动监控                                       │   │
│  │  │ ┌──────────────────┐   │    │  │                                                                           │   │
│  │  │ │  Linux微信客户端   │   │    │  │  ┌──────────────────────────────────────────────┐                      │   │
│  │  │ │  (WeChat Linux)  │   │    │  │  │  WebSocket 实时通信 (意图反馈/沙盒登录引导)  │                      │   │
│  │  │ └──────────────────┘   │    │  │  └──────────────────────────────────────────────┘                      │   │
│  │  └────────────────────────┘    │  └────────────────────────────────────────────────────────────────────────────┘   │
│  └──────────────────────────────┘                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              服务层                                                         │
│                                                                                          │
│  ┌──────────────────────────────────────────────────────────────────────────────────┐  │
│  │                   LangGraph Workflow Engine (AI Agent工作流引擎)                  │  │
│  │            ─────────────────────────────────────────────────                    │  │
│  │            LangGraph 0.2.55+ (Orchestrator-Worker模式)                          │  │
│  │            LangChain 0.3.14+ (连接器集成)                                         │  │
│  │            Pydantic 2.9.2+ (状态Schema定义)                                       │  │
│  │                                                                                  │  │
│  │  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐                    │  │
│  │  │ Intent       │────▶│ Sandbox      │────▶│ Visual        │                    │  │
│  │  │ Recognizer   │     │ Launcher     │     │ Locator      │                    │  │
│  │  │ (LLM意图识别) │     │ (启动监控)    │     │ (视觉定位)    │                    │  │
│  │  └──────────────┘     └──────────────┘     └──────────────┘                    │  │
│  │        │                     │                     │                             │  │
│  │        │ 扫码登录            │ 返回交互层          │ 流式消息                      │  │
│  │        └─────────────────────┘─────────────────────┘                             │  │
│  │                              ▼                                                     │  │
│  │  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐                    │  │
│  │  │ Multimodal   │────▶│ Agent        │────▶│ Document     │                    │  │
│  │  │ Processor   │     │ Decision     │     │ Executor     │                    │  │
│  │  │ (多模态理解) │     │ (核心决策)    │     │ (执行操作)    │                    │  │
│  │  └──────────────┘     └──────────────┘     └──────────────┘                    │  │
│  │        │                     │                     │                             │  │
│  │        │                     │ 自主决策             │ 调用工具                     │  │
│  │        │                     │                     │                             │  │
│  │        │                     ▼                     ▼                             │  │
│  │        │              ┌──────────────┐     ┌──────────────┐                    │  │
│  │        │              │ WriteReport  │     │ UpdateLedger │                    │  │
│  │        │              │ SaveMessage │     │ CustomAction │                    │  │
│  │        │              └──────────────┘     └──────────────┘                    │  │
│  │        │                                                                                              │  │
│  │        └──────────────────────────────────────────────────────────────────────┘                  │  │
│  │                              ▼                                                     │  │
│  │                    ┌──────────────┐         ┌──────────────┐                     │  │
│  │                    │ Shared       │         │ Conditional  │                     │  │
│  │                    │ State        │         │ Edge         │                     │  │
│  │                    └──────────────┘         └──────────────┘                     │  │
│  └──────────────────────────────────────────────────────────────────────────────────┘  │
│                           │                              │                              │
│                           │ (状态读取/更新)                │ (工具调用)                  │
│                           ▼                              ▼                              │
│  ┌─────────────────────────────────┐    ┌─────────────────────────────┐                │
│  │        支撑服务层                │    │        工具层                                            │   │
│  │        ─────────────────         │    │        ──────────                                       │   │
│  │        FastAPI 0.115.0+          │    │        LangChain Tools (Tool Calling)                     │   │
│  │        Redis 7.4.5+             │    │                                                           │   │
│  │        ChromaDB 0.6.3+          │    │  ┌─────────────────┐  ┌──────────────────────────────┐    │   │
│  │        Ollama 0.5.11+           │    │  │  Excel Tool     │  │      Word Report Tool        │    │   │
│  │        Redis-Checkpointer       │    │  │  (openpyxl)     │  │      (python-docx)           │    │   │
│  │                                 │    │                                                           │   │
│  │  ┌─────────────────┐  ┌──────┐ │    │  │  ┌───────────┐  │  │  ┌─────────────────────────┐  │    │   │
│  │  │ WeChat Sandbox  │  │Chroma│ │    │  │  │ pywin32  │  │  │  │  python-docx-template   │  │    │   │
│  │  │  (Docker容器)    │  │Vector│ │    │  │  │  (UI自动) │  │  │  │  (报告模板)             │  │    │   │
│  │  │  x11vnc + noVNC  │  │ Store│ │    │  │  └───────────┘  │  │  └─────────────────────────┘  │    │   │
│  │  │                 │  │      │ │    │  └─────────────────┘  └──────────────────────────────┘    │   │
│  │  │ ┌───────────┐  │  │      │ │    │                                                           │   │
│  │  │ │ FastAPI   │  │  │      │ │    │  ┌──────────────────────────────────────────────────┐    │   │
│  │  │ │ + SSE     │  │  │      │ │    │  │  Redis Pub/Sub (消息队列/跨工作流通信)        │    │   │
│  │  │ │ Producer  │  │  │      │ │    │  └──────────────────────────────────────────────────┘    │   │
│  │  │ └───────────┘  │  └──────┘ │    └──────────────────────────────────────────────────────────┘   │
│  │  └─────────────────┘           │                                                                   │   │
│  │           │               │     │                                                                   │   │
│  │           └───────────────┼─────┘                                                                   │   │
│  │                           │ (RAG检索)                                                           │   │
│  │                    ┌──────▼──────┐                                                              │   │
│  │                    │ Ollama      │                                                              │   │
│  │                    │ ───────────  │                                                              │   │
│  │                    │ Qwen3-VL:   │                                                              │   │
│  │                    │   视觉理解   │                                                              │   │
│  │                    │ Qwen3-Embed:│                                                              │   │
│  │                    │   向量嵌入   │                                                              │   │
│  │                    └─────────────┘                                                              │   │
│  └─────────────────────────────────┘                                                                   │   │
└─────────────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              数据层                                                         │
│                              ──────────                                                     │
│                              Redis 7.4.5+ (Checkpointer/Pub/Sub)                          │
│                              ChromaDB 0.6.3+ (RAG向量存储)                                 │
│                              文件存储 (Excel/Word/用户数据)                               │
│                                                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │ Redis Queue │  │ ChromaDB    │  │ Excel 台账  │  │ Word 报告   │  │ 微信用户数据 │  │
│  │ 消息队列    │  │ 向量数据库  │  │ (.xlsx)     │  │ (.docx)     │  │ (本地存储)   │  │
│  │ (状态持久化) │  │ (RAG检索)   │  │             │  │             │  │             │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 多实例部署架构

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                              多实例部署架构                                               │
│                                                                                          │
│  ┌──────────────────────────────────────────────────────────────────────────────────┐  │
│  │                         Docker Host                                               │  │
│  │                                                                                  │  │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                  │  │
│  │  │ WeChat Instance │  │ WeChat Instance │  │ WeChat Instance │                  │  │
│  │  │       1         │  │       2         │  │       3         │                  │  │
│  │  │                 │  │                 │  │                 │                  │  │
│  │  │ API: 8001       │  │ API: 8002       │  │ API: 8003       │                  │  │
│  │  │ VNC: 5901       │  │ VNC: 5902       │  │ VNC: 5903       │                  │  │
│  │  │ noVNC: 6081     │  │ noVNC: 6082     │  │ noVNC: 6083     │                  │  │
│  │  └─────────────────┘  └─────────────────┘  └─────────────────┘                  │  │
│  │           │                    │                    │                            │  │
│  │           └────────────────────┼────────────────────┘                            │  │
│  │                                │                                                 │  │
│  │  ┌─────────────────────────────▼───────────────────────────────────────────────┐  │  │
│  │  │                         Shared Redis (消息队列/状态存储)                      │  │  │
│  │  └────────────────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                                  │  │
│  │  ┌────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │                         Orchestrator Service (工作流触发)                     │  │  │
│  │  └────────────────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                                  │  │
│  │  ┌────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │                         Ollama Service (AI模型服务)                          │  │  │
│  │  └────────────────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                                  │  │
│  │  ┌────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │                         ChromaDB Service (向量数据库)                         │  │  │
│  │  └────────────────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

## 3. 架构详述

### 3.1 用户交互层

#### 3.1.1 VNC/noVNC 远程访问

**功能**: 在浏览器中远程访问 Docker 容器内的微信界面

**技术实现**:
- 使用 `jlesage/baseimage-gui` 镜像提供 VNC/noVNC 支持
- noVNC 服务默认运行在 6080 端口
- VNC 服务运行在 5900 端口
- 支持密码认证（通过环境变量配置）

**访问方式**:
- 单实例: `http://localhost:6080`
- 多实例: `http://localhost:6081/6082/6083`

**优势**:
- 无需安装 VNC 客户端
- 跨平台支持（Windows、Mac、Linux）
- 便于远程管理和调试

#### 3.1.2 对话式 Web UI 界面

**功能**: 提供友好的对话式界面，用户通过自然语言配置任务和监控群聊

**主要模块**:
1. **对话界面**: 类似ChatGPT的对话式交互界面
2. **任务配置**: 用户发送工作配置消息，系统解析并保存任务信息
3. **群聊监控**: 用户发送监控指令，系统启动指定群聊的监控
4. **状态显示**: 实时显示当前监控的群聊、任务列表、日报生成状态

MVP阶段
**用户交互流程**:
```
用户:发送 1.工作1（地点A）人员1，人员2，具体工作内容2.工作2（地点B）人员3，人员4，然后监控群聊SSS

系统: 已启动监控群聊"SSS"，正在监听消息...
```

**技术实现**:
- 原生 JavaScript + WebSocket/Fetch API
- 流式响应显示AI回复
- 消息历史记录持久化

**API 端点**:
- `GET /health`: 健康检查
- `POST /chat`: 发送用户消息，获取AI回复（流式）
- `GET /tasks`: 获取当前任务列表
- `GET /monitored-groups`: 获取监控的群聊列表
- `GET /reports`: 获取已生成的日报列表

#### 3.1.3 Orchestrator API

**功能**: 提供工作流管理和触发的 REST API

**主要端点**:
- `POST /chat`: 处理用户对话消息（任务配置、监控指令等）
- `POST /workflow/trigger`: 触发新的工作流实例
- `GET /workflow/{workflow_id}/state`: 获取工作流状态
- `GET /workflow/{workflow_id}/status`: 获取工作流执行状态
- `POST /workflow/{workflow_id}/cancel`: 取消工作流

### 3.2 协调与接口层

#### 3.2.1 Orchestrator Service

**职责**: 系统的HTTP入口和运维面板

**关键功能**:
- 接收来自微信沙盒的触发请求
- 启动新的LangGraph工作流实例
- 提供管理界面（工作流状态、日志、配置）
- 工作流生命周期管理

**关键设计**:
- 核心是一个LangGraph工作流的包装器（Wrapper）和触发器
- 当收到新消息时，初始化一个工作流State并调用 `workflow.invoke()`
- 支持工作流检查点（Checkpoint）和状态持久化

#### 3.2.2 API 服务器生命周期管理

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    global queue_manager, producer1, producer2
    try:
        logger.info("Starting Producer Service...")
        queue_manager = RedisQueueManager()
        producer1 = Producer1Observer(queue_manager)
        producer2 = Producer2ContentFetcher(queue_manager)
        producer1.start()
        producer2.start()
        logger.info("Producer Service started successfully")
        yield
    except Exception as e:
        logger.error(f"Failed to start Producer Service: {e}")
        raise
    finally:
        logger.info("Shutting down Producer Service...")
        if producer1:
            producer1.stop()
        if producer2:
            producer2.stop()
        logger.info("Producer Service stopped")
```

### 3.3 核心处理层：LangGraph工作流引擎

这是系统的大脑和中枢神经，基于LangGraph框架进行智能工作流编排。LangGraph是一个专门为构建有状态、多智能体应用程序而设计的框架，支持复杂的条件路由、循环和状态持久化。

**LangGraph核心优势**:
- **状态管理**: 统一的TypedDict状态对象，数据在工作流节点间流动
- **条件路由**: 基于状态的智能决策，动态选择执行路径
- **检查点机制**: 支持工作流状态持久化，可暂停和恢复执行
- **循环支持**: 处理需要多次迭代的任务（如持续监控群聊消息）
- **可视化**: 工作流图的可视化和调试

#### 3.3.1 工作流状态（State）

一个统一的、强类型的TypedDict（如AgentState），作为数据在整个工作流中流动的载体。

**包含字段**:
- `user_message`: 来自用户对话界面的消息（输入）
- `tasks`: 用户配置的任务列表（工作名称、地点、人员、内容等）
- `monitored_groups`: 监控的群聊列表
- `raw_message`: 来自微信的原始消息（输入）
- `multimodal_analysis`: 多模态分析结果
- `task_status`: 任务状态（如 "waiting_mid"）
- `action_type`: Agent决策的操作类型（"write_report" / "update_ledger" / "save_message" / "continue"）
- `daily_report`: 生成的日报内容
- `document_updates`: 需要执行的文档更新指令（输出）
- `messages`: 用于串联对话的消息记录

**设计意义**:
- 状态对象取代了跨Agent的消息协议
- 使数据流和当前上下文一目了然
- 支持用户配置的任务信息持久化
- LangGraph自动处理状态传递和更新

#### 3.3.2 工作流节点（Nodes）

节点是AI Agent工作流中的处理单元，每个节点实现特定的智能决策能力，通过共享内存状态协同完成复杂的自动化任务。

**Intent Recognizer Node (LLM意图识别节点)**:
- 接收来自用户对话界面的消息（`user_message`）
- 使用LLM解析用户意图，判断是：
  - 任务配置意图（如："1.工作1（地点A）人员1，人员2"）
  - 群聊监控意图（如："监控群聊SSS"）
  - 查询状态意图（如："查看任务进度"）
  - 其他自定义意图
- 根据意图将消息路由到相应的处理节点
- 生成自然语言回复返回给用户

**Sandbox Launcher Node (微信沙盒启动节点)**:
- 当识别到群聊监控意图后，自动启动微信沙盒监控程序
- 检查微信沙盒是否已登录
- **关键交互**：如果未登录，返回交互层提示用户通过noVNC扫码登录
- 登录完成后，自动继续监控流程
- 向微信沙盒发送监控启动指令

**Visual Locator Node (视觉定位节点)**:
- 接收微信沙盒的屏幕截图
- **使用LLM视觉能力**自动识别并定位微信界面中的关键区域：
  - 群聊名称区域（识别用户指定的群聊"SSS"）
  - 消息接收区域（获取最新消息的位置）
  - 消息发送区域（定位输入框和发送按钮）
- 生成区域坐标信息，返回给微信沙盒监控程序
- 启动微信沙盒中的群聊监控程序，**流式获取群聊消息**

**Multimodal Processor Node (多模态理解节点)**:
- 接收来自微信沙盒的流式群聊消息（文本+截图）
- 调用本地多模态LLM（Qwen3-VL）理解图文混合内容
- 提取关键信息：任务状态、人员、地点、工作内容等
- 调用RAG检索相关业务知识（通过ChromaDB）
- 将结构化分析结果写回State的 `multimodal_analysis` 字段

**Agent Decision Node (核心决策节点)**:
- 这是系统的**核心AI Agent**，实现自主决策能力
- 读取 `multimodal_analysis` 分析结果
- 根据消息内容**自主判断**后续操作：
  - **写日报**：当检测到任务完成或工作总结类消息时
  - **改台账**：当检测到任务状态变更或数据更新类消息时
  - **保存消息**：当需要归档重要消息时
  - **等待继续**：普通消息无需特殊处理
- 决策结果写入State的 `action_type` 字段
- 根据决策结果路由到Document Executor Node的不同子节点

**Document Executor Node (文档执行节点)**:
- 根据 `action_type` 调用相应的工具执行操作：
  - **WriteReport**: 调用LLM生成日报，调用Word Tool生成报告
  - **UpdateLedger**: 调用Excel Tool更新本地台账
  - **SaveMessage**: 将消息保存到数据库或文件系统
  - **CustomAction**: 根据需求扩展的自定义操作
- 执行结果写回State，并反馈给用户交互层

#### 3.3.3 工作流构建与编排

基于LangGraph框架，通过StateGraph定义工作流结构和执行逻辑。

**工作流结构设计**:

- **节点定义**: 每个Worker节点实现特定的智能决策能力
- **边连接**: 定义节点之间的执行顺序和依赖关系
- **条件路由**: Agent Decision Node根据状态动态选择执行路径
- **状态持久化**: 通过Redis Checkpoint保存工作流状态，支持中断恢复

**工作流编译示例（带Redis检查点支持）**:

```python
app = workflow.compile(checkpointer=RedisCheckpointSaver(redis_conn))
```

#### 3.3.4 工作流边（Edges）与条件路由

**边定义节点的执行顺序**（A -> B -> C）

**条件边**: 由Agent Decision Node的决策结果决定

**条件路由示例**:

```python
def route_action(state: AgentState) -> str:
    """根据Agent决策的路由到不同操作"""
    action_type = state.get("action_type", "continue")
    
    # Agent自主决策：根据消息内容判断后续操作
    if action_type == "write_report":
        return "document_executor"
    elif action_type == "update_ledger":
        return "document_executor"
    elif action_type == "save_message":
        return "document_executor"
    else:
        # 普通消息无需特殊处理，等待下一条消息
        return END

# 在工作流中添加条件边
workflow.add_conditional_edges(
    "agent_decision",
    route_action,
    {
        "document_executor": "document_executor",
        END: END
    }
)
```

**智能决策流程**:
1. 用户发送消息 → Intent Recognizer识别意图
2. 如果是监控群聊 → Sandbox Launcher启动监控 → Visual Locator视觉定位 → 流式获取消息
3. 获取到群聊消息 → Multimodal Processor理解内容
4. Agent Decision Node根据内容自主判断操作类型
5. 根据action_type路由到Document Executor或结束
6. Document Executor调用相应工具执行操作并反馈结果

### 3.4 工具层

**定义**: 将被复用的、对外的操作（如读写文件、调用API）封装为LangChain Tool

**示例**:
- `UpdateExcelTool`: 更新Excel台账
- `GenerateReportTool`: 生成Word报告
- `RAGRetrievalTool`: 检索相关业务知识

**特点**:
- 在工作流节点中被声明和调用
- 实现了执行能力的模块化和标准化
- 支持工具的参数验证和错误处理

### 3.5 支撑服务层

#### 3.5.1 微信沙盒容器

**架构**: 独立的Docker化服务

**核心组件**:
1. **Linux微信**: 运行在容器内的Deepin Wine微信
2. **虚拟显示**: Xvfb 提供虚拟显示服务器（1920x1080 分辨率）
3. **桌面环境**: Fluxbox 轻量级窗口管理器
4. **生产者服务**: FastAPI服务，负责消息捕获和分发
5. **屏幕检测**: ChangeDetector 检测屏幕变化
6. **消息分类**: MessageTypeClassifier 识别消息类型
7. **VNC服务**: noVNC + x11vnc 提供远程访问能力

**Monitor Agent服务**:
- 管理沙盒生命周期（启动、停止、重启）
- 从SSE流中读取数据
- 向Orchestrator发起HTTP请求，触发工作流

**远程访问**:
- **noVNC Web界面**: 通过浏览器访问（端口 6080），密码默认为 wechat123
- **VNC客户端**: 支持 RealVNC、TightVNC 等客户端（端口 5900），密码默认为 wechat123

**数据流**:
```
微信消息 → 屏幕截图 → ChangeDetector检测 → 消息捕获 → Redis队列 → Monitor Agent → Orchestrator → LangGraph工作流
```

#### 3.5.2 AI与知识库服务

**Ollama**:
- 提供Qwen3-VL和Qwen3-Embedding-4B模型服务
- 本地部署，保护数据隐私
- 支持批量推理和流式输出

**Chroma向量数据库**:
- 存储业务知识嵌入
- 与LangChain生态集成
- 在Multimodal Node中被调用以完成RAG检索

**状态持久化（可选）**:
- LangGraph支持将工作流State检查点（Checkpoint）保存至数据库（如Redis）
- 实现长周期、可中断重启的复杂会话

## 4. 关键数据流（LangGraph范式）

### 4.1 消息捕获与工作流触发

```
微信消息 
  → 沙盒容器捕获 
  → ChangeDetector检测变化 
  → MessageTypeClassifier分类 
  → Producer Service写入Redis队列 
  → Monitor Agent从Redis读取 
  → (HTTP Post) Orchestrator 
  → 创建并invoke新的工作流实例
```

### 4.2 工作流内部执行

```
工作流开始 (State初始化)
    ↓
Monitor Node: 将原始消息载入State.raw_message
    ↓
Multimodal Node: 分析 & RAG → 结果写入State.multimodal_analysis
    ↓
StateTracker Node: 更新状态，并决定下一步
    ├── 若任务未完成 → 工作流结束（等待下次触发）
    └── 若任务完成 → 执行 Document Node
            ↓
        Document Node: 调用工具，更新文档 → 结果写入State
            ↓
        工作流结束
```

### 4.3 状态持久化

每个工作流实例的State可被自动保存为检查点。当同一任务的下一条消息触发新工作流时，可从检查点恢复状态，实现跨消息的连续对话和跟踪。

**状态存储示例**:
```python
# 使用Redis作为状态存储后端
workflow = create_workflow()
app = workflow.compile(checkpointer=RedisCheckpointSaver(redis_conn))
```

## 5. 扩展性设计

### 5.1 动态Worker扩展

基于LangGraph Send API，Orchestrator可动态创建Worker节点处理并行任务：

**动态Worker创建场景**:
- 单用户监控多个群聊时，为每个群聊创建独立的Multimodal Processor Worker
- 多用户并发访问时，为每个用户创建独立的工作流实例
- 批量处理任务时，并行创建多个Document Executor Worker

**优势**:
- Worker状态相互隔离，互不干扰
- 支持异步执行，Orchestrator无需等待Worker完成
- 结果自动聚合到共享State中供Orchestrator决策

### 5.2 节点功能扩展

通过添加新节点扩展系统能力：

**新增Worker节点**:
- Notification Node: 发送邮件、钉钉、Slack通知
- Validation Node: 数据验证和质量检查
- Retry Node: 失败重试和错误恢复
- Analytics Node: 数据分析和报表生成

**修改条件路由**:
- 在Agent Decision Node中添加新的决策分支
- 更新条件边路由逻辑，支持新的执行路径

### 5.3 消息源替换

Monitor Node是输入适配器，支持灵活切换消息源：

**支持的消息源**:
- 微信PC版沙盒（当前实现）
- 企业微信API（未来扩展）
- 飞书、钉钉等IM平台
- 自定义消息队列（Kafka、RabbitMQ）

**扩展方式**:
- 修改外部Monitor Agent服务，适配新的消息源
- 工作流内部无需任何修改
- State结构保持兼容

### 5.4 工具生态集成

工具层通过LangChain Tool机制实现模块化扩展：

**内置工具**:
- Excel Tool: 更新Excel台账
- Word Tool: 生成Word报告
- RAG Tool: 向量检索

**可扩展工具**:
- 搜索工具: Google Search、Bing Search
- 计算工具: Calculator、Python REPL
- 文件操作: Cloud Storage、FTP
- 通知工具: Email、Slack、DingTalk、飞书

**扩展方式**:
- 实现标准Tool接口
- 在Worker节点中声明和调用
- 支持参数验证和错误处理

### 5.5 水平扩展

**服务层扩展**:
- Redis集群: 支持大规模消息队列和状态存储
- Ollama分布式: 支持多个模型实例负载均衡
- ChromaDB集群: 支持向量数据库的水平扩展

**容器编排**:
- Docker Compose: 多实例部署（单机）
- Kubernetes: 生产环境大规模部署
- 支持自动扩缩容和故障恢复

**多用户数据隔离**:
- State增加user_id字段
- Redis使用用户ID作为key前缀
- ChromaDB为每个用户创建独立collection

## 6. 与技术栈的对应关系

本架构是所选技术栈的具象化体现：

- **LangGraph**: 实现了本架构的核心工作流引擎
- **Chroma + Qwen3-Embedding-4B**: 作为Multimodal Node中的检索增强组件
- **LangChain & Tools**: 提供了节点与模型、工具、向量数据库交互的标准方式
- **FastAPI & Docker**: 构成了外围的服务化与容器化基础设施
- **Redis**: 实现消息队列和状态持久化
- **noVNC/VNC**: 实现浏览器远程访问
- **OpenCV**: 实现屏幕变化检测和图像处理

## 7. 安全与权限

### 7.1 访问控制

- VNC密码认证
- API接口Token认证
- Web UI用户登录认证（可选）

### 7.2 数据隔离

- 多实例部署时，每个实例的数据完全隔离
- Redis队列使用不同的key前缀
- ChromaDB使用不同的collection

### 7.3 敏感信息保护

- 所有敏感配置使用环境变量
- 禁止在代码中硬编码密码和密钥
- 日志输出脱敏处理

## 8. 监控与运维

### 8.1 日志管理

- 使用 structlog 生成结构化日志
- 日志级别：DEBUG、INFO、WARNING、ERROR
- 支持日志轮转和远程日志收集

### 8.2 健康检查

- API服务健康检查端点：`GET /health`
- 容器健康检查（Docker healthcheck）
- Redis连接检查

### 8.3 指标监控

- 消息处理延迟
- 队列长度
- 工作流执行次数
- 错误率
- 资源使用率（CPU、内存、磁盘）

### 8.4 告警机制

- 服务异常告警
- 队列积压告警
- 工作流失败告警
- 资源使用超限告警

## 9. 性能优化

### 9.1 消息处理优化

- 异步I/O处理
- 批量消息处理
- 并行工作流执行

### 9.2 AI推理优化

- 模型量化（Qwen3-VL支持INT8量化）
- 批量推理
- 缓存常用结果

### 9.3 数据库优化

- Redis连接池
- ChromaDB索引优化
- 向量检索优化（使用HNSW索引）

## 10. 部署策略

### 10.1 开发环境

- 单实例Docker部署
- 本地Ollama服务
- 本地ChromaDB
- 本地Redis

### 10.2 生产环境

- 多实例Docker部署（3个实例）
- 独立Ollama服务容器
- 独立ChromaDB容器
- 独立Redis容器（支持集群）
- 容器编排（Docker Compose或Kubernetes）

### 10.3 高可用部署

- Redis Sentinel或Redis Cluster
- Ollama多实例负载均衡
- ChromaDB集群
- 工作流检查点持久化
- 自动故障转移

