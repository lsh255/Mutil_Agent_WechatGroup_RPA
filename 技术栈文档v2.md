# 技术栈文档 v2

## 项目概述
- 项目名称：wechat-workflow-agent
- 版本：0.1.0
- 描述：多模态Agent微信群自动化项目 - LangGraph版本
- Python版本：>=3.12

## 技术栈分类

### 1. 核心框架层

| 技术 | 版本 | 用途 |
|------|------|------|
| LangGraph | >=0.0.50 | 工作流编排引擎，构建有状态多智能体系统 |
| LangChain | >=0.1.0 | AI模型调用链、提示词管理、工具集成 |
| LangChain-Community | >=0.0.10 | 集成ChatOllama等社区模型 |
| LangChain-Chroma | >=0.1.0 | 向量数据库连接器 |

**UFO Constellation架构组件：**
| 组件 | 类型 | 用途 |
|------|------|------|
| ConstellationAgent | 全局协调器 | 分解用户意图为DAG任务图，编排多智能体协作 |
| DAGBuilder | DAG构建器 | 根据用户请求类型动态生成任务依赖图 |
| SandboxAuthAgent | 沙盒认证Agent | 管理微信沙盒容器状态和登录流程 |
| UserFeedbackAgent | 用户反馈Agent | 通过WebSocket与前端交互，接收用户反馈 |
| TrackerAgent | 状态跟踪Agent | 维护任务状态，判断任务完成条件 |
| RedisCheckpointer | 状态检查点 | LangGraph状态持久化到Redis |

### 2. Web服务层

| 技术 | 版本 | 用途 |
|------|------|------|
| FastAPI | >=0.104.0 | HTTP服务框架，提供异步支持和自动API文档 |
| Uvicorn | >=0.24.0 | ASGI服务器，运行FastAPI应用 |
| Pydantic | >=2.5.0 | 数据验证、配置管理、API请求响应模型 |
| Pydantic-Settings | >=2.1.0 | 多源配置管理（YAML + 环境变量） |
| WebSocket | - | 前后端实时双向通信，用户交互反馈 |

**API端点扩展：**
- `POST /constellation/request`: 接收用户请求，触发ConstellationAgent
- `POST /constellation/feedback`: 接收前端用户反馈
- `GET /constellation/status`: 获取DAG任务执行状态
- `WebSocket /ws/{user_id}`: WebSocket连接，实时推送交互请求

### 3. 消息队列与状态管理

| 技术 | 版本 | 用途 |
|------|------|------|
| Redis | >=7.2.0 | 消息持久化、分发、状态管理 |
| Redis-Checkpointer | - | LangGraph状态持久化 |

### 4. AI模型层

| 技术 | 用途 |
|------|------|
| ChatOllama (LangChain-Community) | 调用本地Ollama大语言模型 |
| OpenAIEmbeddings (LangChain) | 文本向量化（兼容硅基流动API） |
| Qwen3-VL-8B | 视觉语言模型，理解图文混合内容 |
| Qwen3-Embedding-8B | 文本嵌入模型，用于RAG检索 |
| Qwen3-72B | 大语言模型，用于意图识别和决策 |

### 5. 向量数据库层

| 技术 | 用途 |
|------|------|
| ChromaDB | 向量数据库，存储知识库向量 |
| Chroma (langchain-chroma) | LangChain与ChromaDB的集成 |

### 6. 图像处理与自动化

| 技术 | 版本 | 用途 |
|------|------|------|
| OpenCV-Python | >=4.8.0 | 图像处理、屏幕变化检测、气泡识别 |
| Pillow | >=10.0.0 | 图像操作、截图处理 |

### 7. 文档操作层

| 技术 | 版本 | 用途 |
|------|------|------|
| openpyxl | >=3.1.0 | Excel文档读写和更新 |
| python-docx | >=1.1.0 | Word文档生成 |
| Jinja2 | >=3.1.0 | 模板引擎，报告渲染 |

### 8. 容器与编排层

| 技术 | 版本 | 用途 |
|------|------|------|
| Docker (Python SDK) | >=7.0.0 | 容器生命周期管理 |
| Docker Compose | - | 多容器编排 |

### 9. 日志与监控层

| 技术 | 版本 | 用途 |
|------|------|------|
| structlog | >=23.2.0 | 结构化日志生成 |
| Prometheus-client | >=0.19.0 | 指标收集与暴露 |

### 10. 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.3+ | 前端框架 |
| Vite | 5.4+ | 构建工具 |
| TypeScript | 5.5+ | 类型安全开发 |
| React Router | 6.26+ | 单页应用路由 |
| Zustand | 4.5+ | 状态管理 |
| Tailwind CSS | 3.4+ | CSS框架 |
| Axios | 1.7.7 | HTTP客户端 |
| Recharts | 2.12.7 | 图表库 |

## 项目目录结构

```
Mutil_Agent_WechatGroup_RPA/
├── agents/                           # 智能体模块
│   ├── prompts/                     # 智能体提示词
│   │   ├── decision_prompts.py
│   │   ├── intent_prompts.py
│   │   └── visual_prompts.py
│   ├── decision_agent.py            # 决策智能体
│   ├── intent_agent.py              # 意图识别智能体
│   ├── monitor_agent.py             # 监控智能体
│   └── visual_agent.py              # 视觉定位智能体
├── config/                           # 配置管理
│   ├── settings.py                  # Pydantic配置类
│   └── settings.yaml                # 配置文件
├── core/                             # 核心业务逻辑
│   ├── workflows/                    # LangGraph工作流
│   │   ├── nodes/                   # 工作流节点
│   │   │   ├── document_node.py     # 文档执行节点
│   │   │   ├── monitor_node.py      # 监控节点
│   │   │   ├── multimodal_node.py   # 多模态分析节点
│   │   │   └── state_tracker_node.py # 状态跟踪节点
│   │   └── main_workflow.py         # 主工作流
│   ├── schemas.py                   # 数据模型定义
│   └── state.py                     # LangGraph状态定义
├── docker/                           # Docker配置
│   ├── base/                        # 基础镜像
│   ├── compose/                     # Docker Compose配置
│   │   ├── nginx/
│   │   ├── docker-compose.dev.yml
│   │   ├── docker-compose.multi.yml
│   │   ├── docker-compose.prod.yml
│   │   └── docker-compose.sandbox.test.yml
│   ├── frontend/                    # 前端镜像
│   ├── orchestrator/                # Orchestrator镜像
│   ├── sandbox/                     # WeChat Sandbox镜像
│   └── scripts/                     # 启动脚本
├── docs/                             # 文档
│   ├── ENVIRONMENT_INIT.md
│   └── ENVIRONMENT_SETUP.md
├── frontend/                         # 前端应用
│   ├── src/
│   │   ├── assets/
│   │   ├── constants/
│   │   ├── hooks/
│   │   ├── pages/
│   │   │   ├── admin/              # 管理页面
│   │   │   └── chat/               # 对话页面
│   │   ├── router/
│   │   ├── services/
│   │   ├── store/
│   │   ├── types/
│   │   └── utils/
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── knowledge_base/                    # 知识库管理
│   ├── embeddings.py
│   └── vector_store.py
├── scripts/                          # 脚本
│   ├── create_excel_template.py
│   ├── init_knowledge_base.py
│   ├── start_all.py
│   └── test_workflow.py
├── services/                          # 服务模块
│   ├── orchestrator/                # 协调中心
│   │   └── main.py                  # FastAPI应用入口
│   └── wechat_sandbox/              # 微信沙盒
│       ├── api/                     # API接口
│       ├── archive/                 # 归档文件
│       ├── core/                    # 核心业务逻辑
│       │   ├── classifier/          # 消息分类
│       │   ├── detector/            # 变化检测
│       │   ├── extractor/           # 内容提取
│       │   ├── platform/           # 跨平台适配
│       │   ├── producer/            # 消息生产者
│       │   └── queue/               # 队列管理
│       ├── config.yaml              # 沙盒配置
│       └── main.py                  # 沙盒服务入口
├── pyproject.toml                    # Python项目配置
├── requirements.txt                  # 依赖列表
├── .env.example                      # 环境变量示例
└── .gitignore
```

## 核心数据模型

### LangGraph工作流状态 (AgentState)
```python
class AgentState(TypedDict):
    raw_message: Optional[RawMessage]           # 原始消息
    multimodal_analysis: Optional[MultimodalAnalysis]  # 多模态分析结果
    task_status: Optional[TaskStatus]           # 任务状态
    document_updates: List[DocumentUpdate]      # 文档更新指令
    messages: Annotated[list, add_messages]     # 消息记录
    context: Dict[str, Any]                     # 上下文信息
```

### UFO Constellation工作流状态 (ConstellationState)
```python
class ConstellationState(TypedDict):
    user_request: Optional[UserRequest]           # 用户请求
    dag_graph: Optional[Dict[str, Any]]          # DAG任务图
    task_executions: Dict[str, TaskExecution]     # 任务执行状态
    messages: Annotated[list, add_messages]       # 消息记录
    context: Dict[str, Any]                       # 上下文信息

class UserRequest(TypedDict):
    request_type: RequestType                    # 请求类型
    payload: Dict[str, Any]                      # 请求负载
    user_id: str                                  # 用户ID

class TaskExecution(TypedDict):
    task_id: str                                  # 任务ID
    agent_type: str                               # Agent类型
    status: TaskStatus                            # 任务状态
    result: Optional[Dict[str, Any]]             # 执行结果
    dependencies: List[str]                       # 依赖任务ID

class RequestType(Enum):
    SANDBOX_LOGIN = "sandbox_login"              # 沙盒登录
    WORKFLOW_START = "workflow_start"            # 启动工作流
    WORK_ARRANGEMENT = "work_arrangement"        # 工作安排
```

### 任务状态跟踪 (TaskStatusState)
```python
class TaskStatusState(TypedDict):
    task_id: str                                   # 任务ID
    work_item: str                                 # 事项名称
    work_id: str                                   # 作业ID
    phase: TaskPhase                               # 任务阶段
    completed_phases: List[TaskPhase]             # 已完成阶段
    all_phases_required: bool                      # 是否要求全部阶段
    messages: List[str]                           # 关联消息ID
```

### 数据模型枚举
- **MessageType**: TEXT, IMAGE, MIXED
- **TaskType**: WORK_REPORT, TASK_ASSIGNMENT, STATUS_UPDATE, OTHER
- **TaskPhase**: BEFORE, DURING, AFTER, UNKNOWN

### 核心数据模型
- **RawMessage**: 原始消息（包含发送者、内容、类型、图片路径等）
- **MultimodalAnalysis**: 多模态分析结果（任务类型、阶段、用户、摘要等）
- **TaskStatus**: 任务状态（用户、任务ID、当前阶段、完成状态等）
- **DocumentUpdate**: 文档更新指令（更新类型、目标文件、数据等）

## 配置结构

### 多源配置管理
- 配置文件：config/settings.yaml
- 环境变量：.env
- 支持嵌套配置（使用__分隔符）

### 配置类层次
```
Settings (全局配置)
├── ProjectConfig (项目基础配置)
├── LangGraphConfig (LangGraph工作流配置)
├── AIConfig (AI相关配置)
│   ├── OllamaConfig (Ollama配置)
│   └── SiliconFlowConfig (硅基流动配置)
├── VectorStoreConfig (向量数据库配置)
├── WeChatSandboxConfig (微信沙盒配置)
├── ToolsConfig (工具配置)
├── RedisConfig (Redis配置)
└── LoggingConfig (日志配置)
```

## 工作流节点

### 单Agent工作流节点（向后兼容）
1. **monitor_node**: 监控节点，接收原始消息
2. **multimodal_node**: 多模态分析节点，调用AI模型分析消息
3. **state_tracker_node**: 状态跟踪节点，管理任务状态
4. **document_node**: 文档执行节点，调用工具更新文档

### UFO Constellation工作流节点（新增）
1. **intent_parser_node**: 意图解析节点，解析用户请求类型
2. **dag_builder_node**: DAG构建节点，生成任务依赖图
3. **task_orchestrator_node**: 任务编排节点，分发任务到各Agent
4. **result_collector_node**: 结果收集节点，汇总任务执行结果

### 交互节点（新增）
1. **sandbox_auth_node**: 沙盒认证节点，管理微信沙盒登录流程
2. **user_feedback_node**: 用户反馈节点，通过WebSocket接收用户交互
3. **tracker_node**: 状态跟踪节点，维护任务状态并判断完成条件

## 工作流流程

### 单Agent工作流（向后兼容）
```
Entry → monitor → multimodal → state_tracker → document → END
                                          ↓
                                       (条件分支)
                                    任务完成？
                                    Yes → document
                                    No → END
```

### UFO Constellation工作流（新增）
```
Entry → intent_parser → dag_builder → task_orchestrator → result_collector
                                                              ↓
                                                         (条件分支)
                                                      继续执行？
                                                      Yes → task_orchestrator
                                                      No → END
```

### 多Agent交互工作流（新增）
```
sandbox_auth → user_feedback → (完成) → END
     ↓
monitor_agent → multimodal_agent → tracker_agent → document_agent → END
```

## 智能体模块

### 单Agent工作流智能体（原有）
1. **IntentAgent**: 意图识别智能体
   - 识别用户消息意图
   - 使用ChatOllama调用Qwen3-72B模型
   - 返回JSON格式的意图识别结果

2. **VisualAgent**: 视觉定位智能体
   - 定位微信界面区域
   - 识别群聊名称、消息区域

3. **DecisionAgent**: 决策智能体
   - 根据消息内容自主决策后续操作
   - 支持写日报、更新台账、保存消息等操作

4. **MonitorAgent**: 监控智能体
   - 管理微信沙盒监控
   - 将数据触发至工作流

### UFO Constellation智能体（新增）
1. **ConstellationAgent**: 全局协调器
   - 接收前端用户请求（工作安排、群聊配置、登录请求等）
   - 解析用户意图，生成DAG任务图
   - 使用LangGraph Send API分发任务到各Agent
   - 监控任务执行状态，动态调整DAG

2. **SandboxAuthAgent**: 沙盒认证Agent
   - 检查微信沙盒容器状态
   - 触发微信登录流程
   - 通知前端用户扫码登录
   - 管理VNC连接

3. **UserFeedbackAgent**: 用户反馈Agent
   - 通过WebSocket向前端推送交互请求
   - 接收用户反馈（扫码确认、意图确认等）
   - 将用户反馈返回给ConstellationAgent

4. **TrackerAgent**: 状态跟踪Agent
   - 维护任务状态（替代原有StateTrackerNode）
   - 判断任务是否完成
   - 决定是否生成文档
   - 支持多阶段任务跟踪（前中后）

## 微信沙盒双生产者架构

1. **Producer1 (Observer)**: 消息观察者
   - 检测消息气泡生成小截图
   - 推送到原始消息队列

2. **Producer2 (ContentFetcher)**: 内容获取者
   - 从原始队列消费消息
   - 精确获取消息内容（文本/媒体）
   - 推送到精确消息队列

## API端点

### Orchestrator服务
- `GET /`: 根路径
- `GET /health`: 健康检查
- `POST /workflow/trigger`: 触发工作流执行
- `GET /workflow/status`: 获取工作流状态

### WeChat Sandbox服务
- `GET /stream`: SSE流式接口，推送消息
- `POST /config`: 配置管理
- `POST /instance`: 服务实例管理
- `GET /health`: 健康检查

## 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| pytest | >=7.4.0 | 测试框架 |
| pytest-asyncio | >=0.21.0 | 异步测试 |
| black | >=23.0.0 | 代码格式化 |
| ruff | >=0.1.0 | 代码检查 |
| mypy | >=1.6.0 | 类型检查 |

## 版本约束

- Python: >=3.12
- LangGraph: >=0.0.50
- LangChain: >=0.1.0
- FastAPI: >=0.104.0
- Redis: >=7.2.0
- OpenCV: >=4.8.0
